<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Spooky Countdown</title>
  <style>
    body {
      margin: 0;
      background-color: black;
      color: red;
      font-family: 'Courier New', Courier, monospace;
      text-align: center;
      padding-top: 20%;
      font-size: 2em;
      transition: background-color 0.3s;
    }
    #countdown {
      font-size: 4em;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div>👻 Spooky forces activating in...</div>
  <div id="countdown">5</div>

  <!-- Optional audio jump scare -->
  <audio id="scream" src="scream.mp3" preload="auto"></audio>

  <script>
    let timeLeft = 5;
    const countdownEl = document.getElementById('countdown');
    const scream = document.getElementById('scream');

    const countdown = setInterval(() => {
      timeLeft--;
      countdownEl.textContent = timeLeft;

      if (timeLeft <= 0) {
        clearInterval(countdown);
        document.body.style.backgroundColor = "red";
        countdownEl.textContent = "BOO! 👹";
        scream.play(); // Play scary sound
        flashScreen();  // Optional visual effect
      }
    }, 1000);

    // Optional flashing effect
    function flashScreen() {
      let flashes = 6;
      let flashing = setInterval(() => {
        document.body.style.backgroundColor = 
          document.body.style.backgroundColor === "black" ? "red" : "black";
        flashes--;
        if (flashes <= 0) clearInterval(flashing);
      }, 300);
    }
  </script>
</body>
</html>
